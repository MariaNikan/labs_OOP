#pragma once

using namespace std;

string ReplaceSymbol(std::string& line, char symbol, std::string text);
string HtmlEncode(std::string const& text);
void PrintOutString(std::ostream& stream, std::string& text);
